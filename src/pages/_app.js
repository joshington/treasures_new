import "@/styles/globals.css";

import { ContractKitProvider, useContractKit } from '@celo/contractkit';


export default function App({ Component, pageProps }) {
  return (
    
      <Component {...pageProps} />
    
 
  )
}
